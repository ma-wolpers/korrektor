# Development Log

## [Unreleased]
- Meilenstein 3 der Korrektor-Wunschliste (Punkte-/Noten-Superposition): neue reine Funktion `build_scored_annotation_clones` (`annotation_sync.py`) - wie `build_annotation_clones`, aber `content` kommt aus einem `content_by_student_id`-Dict statt vom `base` uebernommen zu werden, Position/Stil bleiben geteilt; die bestehende Sync-Gruppen-Stilpropagierung (0.3) fasst `content` nirgends an, dafuer ein expliziter Test ergaenzt. Neue `UiIntentControllerSuperpositionMixin` (`ui_intent_controller_superposition.py`): `apply_scored_superposition_immediate`/`apply_grade_superposition_immediate` sind Bulk-Aktionen ueber alle Korrektur-Schueler:innen des aktuellen Bereichs (Ziel-Set wie "Durchdruecken", keine Filterbedingung wie beim Supersymbol) mit striktem Erst-validieren-dann-ausfuehren (`_resolve_superposition_targets_or_none`): fehlt fuer irgendeine Zielperson ein Punktwert (`score_filter.compute_student_value`) oder schlaegt bei der Noten-Variante `resolve_grade` fehl (`GradingError`, z. B. unter der niedrigsten Schwelle), wird die gesamte Aktion mit einer Person/Aufgabe/Bereich-benennenden Fehlermeldung abgelehnt, bevor auch nur eine Annotation angelegt wird - beide teilen sich denselben `_save_annotation_mutation_immediate`-Tail (0.3) fuer Persistenz/Rollback/History. Note-Variante lehnt ohne `exam.grading_scale_snapshot` sofort ab. GUI: neuer Bereich "Superposition" in `main_window_supersymbol.py` (wiederverwendet die bestehende Aufgabe(n)-Scope-Auswahl und den Supersymbol-Superseiten-Render-/Klick-Mechanismus, `_cancel_supersymbol_filter` auf beide Faelle generalisiert), neuer Klick-Zweig in `_on_correction_canvas_press` (`main_window_correction_markers.py`). Getestet in `tests/test_superposition_immediate.py` (Personalisierung, Undo/Redo als eine Aktion, Alles-oder-nichts bei fehlendem Wert/GradingError, Stil-Aenderung laesst Content unangetastet) sowie per Widget-Smoke-Test.
- Meilenstein 2.4/2.5 der Korrektor-Wunschliste, GUI: neues Popup "Notenschluessel verwalten" (`main_window_grading_scale.py: MainWindowGradingScaleMixin`, analog zum Extraseiten-Popup-Muster in `main_window_extra_pages.py` - `ui.Toplevel` + `_register_popup_window`) mit Treeview-Liste, Formular (Name/Fach/Jahrgang/Schule/Skalentyp-Combobox) und Notenstufen-Eingabe im selben `CODE:Wert`-Zeilenformat wie der bestehende Aufgaben-Editor (`Prozent:Note`, z. B. `80:1`), Verwendungsuebersicht pro Vorlage sowie Archivieren-/Reaktivieren-/Loeschen-Buttons (Loeschen nur bei nie verwendeten Vorlagen aktiv). Neue Zuordnungszeile in der Klausur-Detailansicht (`main_window_view_state.py`): Combobox (nur `status=active`) + "Zuordnen"-Button, Statuslabel zeigt Name/Zuordnungsdatum des aktuellen Snapshots; `_refresh_grading_scale_assignment_choices` wird von `open_exam_detail`, `refresh_exam_content_in_place` (0.1-Refresh-Pfad) sowie nach jeder Verwaltungsaktion aufgerufen. Neue Konstanten `GRADING_SCALE_TYPE_*` in `main_window_constants.py` (Label<->Skalentyp-Mapping, analog `SUPERSYMBOL_OPERATOR_BY_LABEL`). Verifiziert per End-to-End-Smoke-Test durch den echten Widget-Baum (Skala anlegen -> Klausur oeffnen -> zuordnen -> Strg+Z, Label aktualisiert sich korrekt zurueck) statt formaler pytest-Faelle fuer die reine Tkinter-Verdrahtung (Konvention wie bei Meilenstein 1.1-1.3). `main_window_view_state.py` lag mit dem neuen Zuordnungsformular ueber der ~300-Zeilen-Konvention (bereits vorher bei 308 Code-Zeilen) - View-/Modus-Wechsel-Logik (`_show_view`, `_set_detail_submode`, `_return_to_overview`, ...) dafuer in eine neue Datei `main_window_view_transitions.py` ausgelagert (echte zweite Verantwortlichkeit: Widget-Aufbau vs. Sichtbarkeits-Umschaltung), `main_window_view_state.py` bleibt fuer `_build_detail_view`.
- Meilenstein 2.4/2.5 der Korrektor-Wunschliste, Controller-Ebene (noch ohne GUI): neue `UiIntentControllerGradingScaleMixin` (`ui_intent_controller_grading_scale.py`) - CRUD fuer globale `GradingScale`-Vorlagen (`save_grading_scale_immediate`, `archive_/reactivate_grading_scale_immediate`, `delete_grading_scale_immediate`) laeuft bewusst **ausserhalb** der Exam-Undo/Redo-History (keine offene Klausur betroffen, reiner globaler Store); `delete_grading_scale_immediate` lehnt bereits verwendete Vorlagen ab (`GradingScaleRepository.has_usage`) und verweist auf Archivieren. `assign_grading_scale_immediate` (2.5) ist dagegen ein normaler Exam-Content-Mutator: snapshottet die Vorlage in `exam.grading_scale_snapshot`, haengt einen `GradingScaleUsageEntry` an (bewusst ausserhalb der Undo/Redo-History - bleibt beim Rueckgaengigmachen der Zuordnung als Fakt bestehen) und registriert eine `_record_exam_payload_action`. Noch offen: die GUI dafuer (Notenschluessel-Verwaltungs-Popup, Zuordnungs-Dropdown in der Detailansicht).
- Meilenstein 2.7 der Korrektor-Wunschliste: neue `_reject_max_points_change_for_scored_tasks` in `ui_intent_controller_regions.py`, aufgerufen von `upsert_region_immediate` (der einzigen Schreibstelle fuer `RegionAssignment.tasks`) - vergleicht bei einer bestehenden Region (`region_id` bekannt) die neuen gegen die alten `max_points` je `task_code` und lehnt die Aenderung mit `messagebox.showerror` ab, wenn `ScoreRepository.load_scores` fuer diesen Code bereits einen Punktwert fuer irgendeine Person zeigt; nur die tatsaechlich geaenderten Codes werden geprueft, unbetroffene Aufgaben derselben Region bleiben frei editierbar. Neue Region (`region_id=None`) ist nie betroffen. Getestet in `tests/test_upsert_region_immediate.py`.
- Meilenstein 2.1/2.2/2.3/2.6 der Korrektor-Wunschliste (Notenschluessel-Fundament, Variante B): neue Domain-Datei `app/core/domain/grading_scale.py` mit `GradingScale` (globale, wiederverwendbare Vorlage, `status` `active`/`archived`), `GradeThreshold` (`min_percent` + `grade_label`), `GradingScaleSnapshot` (per `GradingScale.to_snapshot()` erzeugte, unabhaengige Kopie) und `GradingScaleUsageEntry` (Append-only-Verwendungsprotokoll). `ExamProject.grading_scale_snapshot` (neues Feld, `models.py`) speichert diese Kopie direkt im Exam statt einer `grading_scale_id`-Referenz - eine spaetere Bearbeitung/Archivierung der globalen Vorlage kann eine bereits ausgewertete Klausur damit nie rueckwirkend veraendern (Ende-zu-Ende dafuer verifiziert in `tests/test_grading.py::test_grade_snapshot_end_to_end_is_immune_to_later_global_scale_edits`, inkl. echtem `to_dict()`/`from_dict()`-Roundtrip). Reine Fachlogik `resolve_grade` (`app/core/domain/grading.py`): prozentual (`achieved/max*100`, 2 Nachkommastellen wie `score_filter.py`s `_POINTS_PRECISION`), Schwellen absteigend sortiert ausgewertet ("hoechste erreichte Schwelle gewinnt", exakter Treffer zaehlt fuer die hoehere Schwelle), `max_points <= 0` oder keine erreichte Schwelle wirft `GradingError` statt still einen Default zu liefern. Persistenz: neuer Port `GradingScaleRepository` (`app/core/ports/repositories.py`), `JsonGradingScaleRepository` (`app/infrastructure/repositories/`) verwaltet `grading_scales.json` und `grading_scale_usage.json` gemeinsam im globalen `%APPDATA%/<app_name>/`-Ordner (bewusst getrennt vom konfigurierbaren Exam-Indexordner) - `delete_scale` setzt keine Nutzungspruefung durch, das ist Aufgabe der aufrufenden Verwaltungs-Ebene (2.4, noch offen). In `app/adapters/bootstrap/wiring.py` verdrahtet (`GuiDependencies.grading_scale_repository`). Noch offen aus Meilenstein 2: 2.4 (Verwaltungs-UI inkl. Verwendungsuebersicht/Archivierung), 2.5 (Zuordnung pro Klausur), 2.7 (`max_points`-Historienschutz).
- Meilenstein 1.3 der Korrektor-Wunschliste: neue `detach_annotation_from_sync_immediate` (`UiIntentControllerAnnotationsMixin`) loest genau eine Annotation aus ihrer Sync-Gruppe (`sync_group_id = ""`), ohne die anderen Mitglieder zu beruehren - im Unterschied zu `disable_annotation_sync_immediate` ("Durchdruecken deaktivieren"), das die gesamte Gruppe aufloest und alle anderen Kopien loescht. Neuer Button "Entkoppeln" in `main_window_correction_markers_transform.py`, Handler `_detach_selected_annotation_from_sync` in `main_window_correction_sync.py`. Getestet: Rest-Gruppe bleibt nach dem Entkoppeln synced (eine nachfolgende Farbaenderung erreicht nur noch das synced gebliebene Mitglied), Undo/Redo-Roundtrip, Ablehnung bei bereits unsynceter Annotation.
- Meilenstein 0.5 der Korrektor-Wunschliste per Tkinter-Testskript empirisch geprueft statt per Codeaenderung "geloest": die vermutete Gefahr, dass `Strg+Z` bei Fokus in einem `Text`-Widget (insbesondere `undo=True`, wie `bw_gui.widgets.WrappedTextField`) nicht bis zu `self.root.bind_all("<Control-z>", ...)` (`main_window_shortcuts.py`) durchdringt, tritt fuer alle im Korrektor tatsaechlich verwendeten Widget-Konfigurationen (`ttk.Entry`, `tk.Text(undo=False)`, `ttk.Combobox`, `tk.Canvas`) sowie zusaetzlich fuer `tk.Text(undo=True)` **nicht** ein, verifiziert an einem sichtbaren/fokussierten Testfenster mit `event_generate("<Control-z>")`. Ein erster Testlauf an einem `withdraw()`-Fenster hatte faelschlich "nicht erreicht" fuer Text/Canvas gemeldet - ein Fokus-Artefakt des versteckten Fensters, kein echtes Bindungsproblem. Kein Code-Fix noetig.
- Meilenstein 1.1/1.2 der Korrektor-Wunschliste: `_supersymbol_scope_combo` (`main_window_supersymbol.py`) bekommt eine `<<ComboboxSelected>>`-Bindung (`_on_supersymbol_scope_changed`), die zusammen mit dem bestehenden Bereichswechsel-Pfad (`_refresh_supersymbol_scope_choices`) den Punktwert ueber die neue `_prefill_supersymbol_value_for_scope` auf die Summe der `TaskDefinition.max_points` der aktuell gewaehlten Aufgabe(n) vorbefuellt. Button-Label in `main_window_correction_core.py` von "PDF ueberschreiben" auf "Alle PDFs ueberschreiben" geaendert (rein kosmetisch, `_save_correction_annotations_to_pdfs` schrieb schon immer alle Schueler:innen-PDFs). Verifiziert per Smoke-Test (vollstaendiger Widget-Aufbau von `MainWindow` inkl. aller neuen/verschobenen Mixins ohne Ausnahme, `window.update()` erfolgreich) statt vollem interaktivem Klick-Durchlauf (keine Display-Automatisierung in dieser Umgebung verfuegbar).
- Meilenstein 0.2/0.3/0.4 der Korrektor-Wunschliste - einzelne Annotationsaktionen werden sofort persistiert und atomar rueckgaengig machbar: neue `UiIntentControllerAnnotationsMixin` (`app/adapters/gui/ui_intent_controller_annotations.py`) mit je einer `<verb>_annotation_immediate`-Methode fuer Platzieren, Loeschen (inkl. ganzer Sync-Gruppe), Groesse, Rotation, Farbe, Durchdruecken ein-/ausschalten sowie `commit_annotation_move_immediate` fuer das Verschieben; alle folgen dem Muster Snapshot-vor-Mutation -> Mutation -> `save_exam`/`load_exam` -> ein `_record_exam_payload_action`-Aufruf (Referenzvorlage: `apply_super_symbol_immediate`). Geteilter Tail-Helfer `_save_annotation_mutation_immediate` haengt bei einem fehlschlagenden `save_exam()` die in-memory-Mutation ueber einen vom Aufrufer uebergebenen `rollback()`-Callback zurueck (Meilenstein 0.4, gleicher Fehlervertrag wie `rename_students_immediate`), statt einen halb-angewendeten Zustand anzuzeigen. Zwei neue pure Domain-Funktionen in `app/core/domain/annotation_sync.py`: `sync_group_members` (Sync-Gruppen-Zugehoerigkeit, vorher zweimal als GUI-Code dupliziert) und `normalize_rotation_deg` (90-Grad-Snapping, vorher nur als GUI-`staticmethod` erreichbar, jetzt vom Controller mitnutzbar; die GUI-Variante ist ein duenner Wrapper). Root-Cause-Bug-Fix (0.2): `save_exam_immediate` (entfernt, war nur noch aus `_save_correction_annotations_to_pdfs` aufgerufen) berechnete seinen "Before"-Snapshot immer erst NACH bereits erfolgten In-Memory-Mutationen - Undo dieses Eintrags war dadurch ein Noop; seit alle Annotationsaenderungen sich selbst sofort persistieren, ist "PDF ueberschreiben" nur noch fuer den physischen PDF-Export zustaendig. `main_window_correction_markers_clipboard.py` (343 Zeilen Code, ueber der ~300-Konvention) in drei Dateien gesplittet: `main_window_correction_markers_clipboard.py` (Kopieren/Ausschneiden/Einfuegen, reine Lookups, Rendering), `main_window_correction_markers_transform.py` (neu: Groesse/Rotation/Loeschen/Platzieren), `main_window_correction_sync.py` (neu: Durchdruecken-Toggle, Sync-Gruppen-Info) - Mixin-Fassaden-Pattern wie beim frueheren `main_window.py`-Split. Neue Tests: `tests/test_annotation_mutations_immediate.py` (Undo/Redo-Roundtrip pro Aktion, Gruppenaktionen als eine History-Aktion, Rollback bei simuliertem Speicherfehler), Ergaenzungen in `tests/test_annotation_sync.py` fuer `sync_group_members`/`normalize_rotation_deg`.
- Undo/Redo-Root-Cause-Fix (Meilenstein 0.1 der Korrektor-Wunschliste): `UiIntentControllerHistoryMixin._refresh_after_history_action` rief nach jedem Undo/Redo unconditional `sync_current_exam_from_repository()` -> `open_exam_detail()` auf, was `_correction_mode_active`, alle Cursor und die aktive View zurueckgesetzt hat - jedes Rueckgaengigmachen einer Inhaltsaenderung (Supersymbol, Punkte, Bereich, Umbenennen, Fertig-Checkbox) sprang dadurch immer zur Detailuebersicht. `HistoryAction` (`app/adapters/undo/history.py`) hat jetzt ein Feld `context: "content" | "lifecycle"` (Default `"content"`); `UndoHistory.undo()`/`redo()` geben die komplette `HistoryAction` statt nur der Beschreibung zurueck (Signaturaenderung, `tests/test_undo_history.py` angepasst). `_refresh_after_history_action` liest `action.context`: nur bei `"lifecycle"` (Klausur anlegen/loeschen, Ablageordner wechseln - die drei einzigen Call-Sites, die das jetzt explizit setzen, in `ui_intent_controller_overview.py`) bleibt der bisherige volle Navigations-Reset; bei `"content"` (Default, alle uebrigen bestehenden `_record_history_action`/`_record_exam_payload_action`-Aufrufe) laedt die neue `MainWindow.refresh_exam_content_in_place()` (`main_window_overview.py`) die Klausur-JSON neu, ohne Moduszustand zurueckzusetzen, und rendert im Korrekturmodus gezielt nach (`_render_correction_preview`/`_refresh_correction_completion_controls`/`_refresh_correction_sync_info`). Die drei `_FakeApp`-Testdoubles (`tests/test_apply_super_symbol_immediate.py`, `tests/test_person_area_finished.py`, `tests/test_rename_students_immediate.py`) implementieren die neue Methode als No-Op. Noch offen aus Meilenstein 0: 0.2 (totes `save_exam_immediate`-Codeaufraeumen, haengt an 0.3), 0.3 (einzelne Symbolinteraktionen werden eigene atomare History-Aktionen), 0.4 (Rollback bei Persistenzfehlern), 0.5 (Strg+Z-Prioritaet gegenueber nativen Tkinter-Text-Undo-Bindings).
- Datei-Groessen-Split von `main_window.py` (5844 Rohzeilen / ~3670 Zeilen Code) und `ui_intent_controller.py` (1047 Rohzeilen / ~519 Zeilen Code) auf die projektweite ~300-Zeilen-Konvention: reine Struktur-Aenderung, kein Verhaltensunterschied. Beide Dateien sind jetzt duenne Fassaden (`main_window.py` ~30 Zeilen, `ui_intent_controller.py` ~13 Zeilen), die tatsaechliche Logik liegt in `main_window_*.py`- bzw. `ui_intent_controller_*.py`-Mixin-Dateien, zusammengesetzt per Mehrfachvererbung (Konvention identisch zum Schwesterrepo `blattwerk`: `BlattwerkApp(BwBaseWindow)` -> `blatt_ui_*.py`-Mixins). `UiIntentController` wurde in 8 Dateien gesplittet (Base/History-Kernel, Overview, Scoring, Regions, Completion, Naming, Supersymbol), `MainWindow` in 25 Dateien (Base, Layout-Orchestrator, plus je einen Mixin pro fachlichem Bereich: Reading/ReadingCanvas, ExtraPages, Naming, RegionEditor/RegionSpecs, CorrectionCore/Form/Completion/Zoom, CorrectionMarkers/MarkersClipboard/MarkersExport, Supersymbol, Menu, Shortcuts/ShortcutsDebug, Theme, Dispatch, Overview, ViewState). Mixins definieren bewusst kein eigenes `__init__` (nur `MainWindowBase`/`UiIntentControllerBase` haben eines); zwei Helper (`_marker_color_name_for_hex`, `_estimate_annotation_rect`), die zuvor `MainWindow.<name>` per hartkodiertem Klassennamen aufriefen, wurden zu `classmethod`s mit `cls.<name>` umgestellt, um einen Zirkelimport zurueck zur Fassade zu vermeiden - identische Laufzeitaufloesung. `main_window_region_editor.py` bleibt mit ~391 Zeilen bewusst ueber der Konvention (dokumentierte Ausnahme in `docs/ARCHITEKTUR.md`: geteilter Treeview-/Selektions-/Draft-Mechanismus von Reading- und Extraseiten-Modus, der sich nicht modusbasiert aufteilen laesst, ohne die Kopplung nur auf mehrere Dateien zu verteilen). Verifiziert nach jedem der 24 Teilschritte per Testsuite (292 Tests durchgehend gruen), zusaetzlich per temporaerem AST-Skript (Duplicate-Check ueber alle neuen Dateien, Vorher-/Nachher-Methodeninventar-Diff gegen eine vor dem Split erstellte Baseline, automatisierter `__mro__`-Check gegen die geplante Reihenfolge) sowie nach den risikoreichsten Schritten (Modus-Exklusivitaets-Vertrag, kompletter `_build_layout`-Split) durch echten App-Start mit Klick-Durchlauf aller vier Modi (Screenshots pixelidentisch zum Stand vor dem jeweiligen Schritt, inkl. des Bereichs-Superbuttons).
- Bereichsweiter Fertig-Superbutton eingefuehrt: `UiIntentController.set_person_area_finished_immediate` wurde zu `set_persons_area_finished_immediate` generalisiert (nimmt jetzt `student_ids: Sequence[str]` statt eines einzelnen `student_id`; bei genau einer ID unveraendertes Verhalten inkl. Fehlermeldungen/Status-/History-Texte, bei mehreren eine einzelne `HistoryAction` fuer alle Aenderungen zusammen). Validierung bleibt alles-oder-nichts: eine unbekannte ID in `student_ids` verwirft die gesamte Aktion, statt die bekannten IDs best-effort zu uebernehmen. Neue reine Zaehlfunktion `count_persons_area_finished` (ein Scan ueber `person_area_completions`, ein Scan ueber `student_ids` - O(Completions + Personen) statt N×M). `MainWindow._are_all_tasks_scored` (neu, `@staticmethod`, kein CSV-I/O) ersetzt die pro-Aufgabe-`load_saved_points`-Schleife von `_all_tasks_scored_for_current_area` durch eine Pruefung auf bereits einmal geladenen `load_scores_for_exam`-Daten (wiederverwendet `compute_student_value` aus dem Supersymbol-Feature rein als Vollstaendigkeits-Check); `_all_tasks_scored_for_current_area` bleibt als duenner Wrapper fuer den Einzel-Checkbox-Handler bestehen. `_refresh_correction_completion_controls` laedt `scores` jetzt genau einmal pro Aufruf und bedient damit sowohl die Einzel- als auch die neue Bereichs-Checkbox. Der neue Superbutton ist ein echter Tri-State (`ttk.Checkbutton`-`alternate`-Status): 0/N zeigt unchecked und ein Klick setzt alle (nur aktivierbar, wenn wirklich alle Personen alle Aufgaben bewertet haben), ein gemischter Zwischenstand zeigt `alternate` und ein Klick setzt alle zurueck auf offen (immer erlaubt, auch wenn zwischenzeitlich nicht mehr alle Aufgaben bewertet sind), N/N zeigt checked und ein Klick setzt ebenfalls alle zurueck. Der Klick-Handler `_on_correction_finished_all_toggled` liest dafuer nie den (bei Tri-State ohnehin nicht eindeutigen) rohen Tkinter-Checkbox-Wert, sondern leitet die Absicht ausschliesslich aus dem tatsaechlichen `finished_count`/`total` her. Getestet in `tests/test_person_area_finished.py` (Controller-Ebene: Einzel-ID-Aequivalenz, Mehrfach-ID als eine Undo/Redo-Aktion, Ablehnung bei unbekannter ID ohne Teiländerung, Dedup-Verhalten von `set_persons_area_finished_immediate` vs. dokumentiertes Ueberzaehl-Verhalten von `count_persons_area_finished` bei Duplikaten, sowie `MainWindow._are_all_tasks_scored` inkl. Vacuous-Truth-Guard bei leerer Personenliste).
- Supersymbol-Filter im Korrekturmodus eingefuehrt: reine Filterlogik in `app/core/domain/score_filter.py` (`compute_student_value`, `matches_condition` mit 2-Nachkommastellen-Praezision fuer `==`, `filter_matching_student_ids`), Bulk-Klon-Mechanismus in `app/core/domain/annotation_sync.py: build_annotation_clones` (jetzt auch von der bestehenden "Durchdruecken"-Funktion `_enable_selected_annotation_sync` genutzt, statt eigenem Klon-Code). `MainWindow._render_superposed_page` wurde in eine reine Pixmap-Berechnung (`_build_superposed_pixmap`) und die bestehende Reading-Canvas-Anzeige aufgeteilt; eine neue `_render_supersymbol_filtered_page` nutzt dieselbe Berechnung fuer die Korrektur-Canvas (volle Seite als Clip, damit `_canvas_to_pdf_coords` ohne Sonderfall funktioniert). `UiIntentController.apply_super_symbol_immediate` (neu) legt fuer alle Treffer je eine `PdfAnnotation` mit gemeinsamer `sync_group_id` an, als eine `HistoryAction`. `CsvScoreRepository.load_scores` (neu, im `ScoreRepository`-Port ergaenzt) liest alle Punktwerte auf einmal statt pro Aufgabe/Person. 0 Treffer bricht mit Fehlermeldung ab, bevor irgendetwas veraendert wird. Getestet in `tests/test_score_filter.py`, `tests/test_annotation_sync.py`, `tests/test_apply_super_symbol_immediate.py`, sowie per Skript gegen die echte Tkinter-Oberflaeche (Filter-Treffer, Klick-Platzierung, Undo, 0-Treffer-Abbruch, "Summe aller Aufgaben"-Scope).
- `ProgressCalculator.compute` gefixt: `corrected_region_count`/`correction_percent` lasen bisher `RegionAssignment.is_corrected`/`ExtraPageAssignment.is_corrected` - Felder, die im gesamten Programm nirgends geschrieben werden (verifiziert per repo-weitem Grep), daher immer 0. Beide Kennzahlen werden jetzt aus `fully_finished_area_count`/`total_area_count` abgeleitet (derselbe `PersonAreaCompletion`-basierte Fortschritt, den auch die Klausur-Detailansicht schon korrekt zeigt). `is_corrected` bleibt im Datenmodell (Persistenzformat nicht angefasst), wird aber ausdruecklich als toter Legacy-Wert dokumentiert. Regressionstests in `tests/test_progress.py`.
- Namenmodus eingefuehrt (`ExamProject.name_region`/`name_region_page`, neue Region-Definitions- und Namenserfassungs-Ansicht in `app/adapters/gui/main_window.py`, Orchestrierung in `UiIntentController.set_name_region_immediate`/`rename_students_immediate`). Die Umbenennung ist eine rollback-faehige Gesamtoperation: 1:1-Preflight des Rename-Sets gegen den Ordnerinhalt (`app/core/domain/rename_planning.py: validate_rename_set`), Zielnamen-Planung inkl. interner Kollisionsaufloesung (`plan_target_filenames`) und externer Konfliktpruefung (`find_external_conflicts`), danach ein zweiphasiges Staged-Rename ueber eindeutige Temp-Namen (`UiIntentController._apply_staged_renames`, loest A<->B-Tauschfaelle kollisionsfrei und rollt bei jedem Fehler bereits ausgefuehrte Schritte zurueck), erst danach In-Memory-Update + `save_exam` + eine einzelne `HistoryAction`, deren Undo/Redo dieselben konkreten alt/neu-Dateinamen-Paare abspielen statt sie neu zu berechnen. `MainWindow.invalidate_doc_cache` schliesst betroffene `fitz.Document`-Handles vor jedem Rename (Windows sperrt offene Dateien). `_render_superposed_page` (Superseite) wurde dabei generalisiert: nimmt jetzt eine explizite `students`-Liste statt fest `exam.students`, damit Einlesemodus und Namenmodus denselben Renderer ohne Duplikat nutzen (Grundlage fuer die Supersymbol-Superseite in einem spaeteren Schritt). Ausfuehrlich getestet: `tests/test_file_utils_naming.py`, `tests/test_rename_planning.py`, `tests/test_staged_rename.py`, `tests/test_rename_students_immediate.py`.
- Regionen-Identitaet entkoppelt: `PersonAreaCompletion`/`PdfAnnotation` referenzieren jetzt `region_id` (bereits vorhandene, stabile UUID auf `RegionAssignment`) statt des mutable `assigned_area_codes`-Labels; `_correction_templates` ist jetzt `dict[region_id, CorrectionTemplate]`, die Bereichs-Auswahl-UI loest Labels ueber eine neue `_correction_label_to_region_id`-Zuordnung auf. `delete_region_immediate` numeriert verbleibende Regionen nicht mehr pauschal nach Listenindex neu (Root Cause dafuer, dass geloeschte Regionen fremde Labels/Referenzen verschoben haben); `_next_area_label` ermittelt das naechste freie Label jetzt kollisionssicher aus tatsaechlich vergebenen Codes statt aus einer reinen Zaehlung (Root Cause des gemeldeten "nur ein Bereich sichtbar"-Bugs). Neue Struktur-Validierung beim Laden (`app/core/domain/validation.py`) und eine Legacy-Migration fuer alte `area_code`-Referenzen (`app/infrastructure/repositories/legacy_migration.py`) ergaenzt; `ListExamsUseCase` isoliert eine fehlerhafte Klausur-Datei jetzt pro Datei statt die gesamte Uebersicht abzubrechen.
- Einlesemodus-Speicherpfad beschleunigt: `UiIntentController._record_exam_payload_action` deep-kopierte die Undo/Redo-Snapshots ein zweites Mal, obwohl `ExamProject.to_dict()` bereits vollstaendig frische, referenzfreie Strukturen liefert (`app/adapters/gui/ui_intent_controller.py`); das redundante `copy.deepcopy(...)` wurde entfernt (Vertrag: Aufrufer uebergeben `to_dict()`-Snapshots direkt). Zusaetzlich lud `refresh_exam_overview` nach jeder Aenderung saemtliche Klausur-JSONs im Indexordner ein zweites Mal, nur um bereits im `ListExamsUseCase`-Ergebnis vorhandene Felder (`exam_id`/`exam_name`) nachzuschlagen; `ExamOverview` traegt jetzt zusaetzlich `exam_file`, sodass der zweite Lade-Durchlauf entfaellt. Offener Folgepunkt (bewusst nicht in diesem Fix behoben): `ListExamsUseCase.execute()` laedt weiterhin bei jeder Einzeländerung alle Klausuren im Ordner neu und berechnet deren Fortschritt komplett neu (O(Anzahl Klausuren) pro Speichervorgang) — eine inkrementelle, nur-das-geaenderte-Exam-aktualisierende Overview waere der naechste Schritt, wurde hier aber nicht vorgezogen, um keine verfruehte Caching-Architektur einzufuehren.
- Menueleisten-Standardisierung gestartet: `app/adapters/gui/main_window.py` baut die Top-Level-Menues jetzt ueber den neuen zentralen `bw_gui.menu.standard_menu`-Baukasten auf (Core-Reihenfolge Datei/Bearbeiten/Ansicht/Hilfe + app-spezifisches `Debug` als Erweiterungssektion).
- Allgemeiner Soft-Guardrail fuer Shortcut-Abdeckung ergaenzt: `tools/ci/check_ai_guardrails.py` meldet lokal (non-blocking) Warnungen, wenn konfigurierte Kern-Intents (u. a. Create/Export/Escape/Undo/Redo/Copy/Cut/Paste/Debug) ohne passenden Keyboard-Binding-Marker gefunden werden.
- Phase-I-Decommission abgeschlossen: die zentralen UI-Contract-Bridges (`bw_libs/ui_contract/keybinding.py`, `bw_libs/ui_contract/popup.py`, `bw_libs/ui_contract/hsm.py`, `bw_libs/ui_contract/laufkern.py`) wurden auf schlanke Shared-Re-Exports reduziert; tote lokale Duplikat-Implementierungen sind entfernt.
- Guardrail-Decommission-Gate aktiviert: `tools/ci/check_ai_guardrails.py` erzwingt jetzt in allen vier Bridge-Modulen `ensure_bw_gui_on_path` + Shared-Import und blockiert lokale Re-Implementierungsmarker der Contract-Klassen/Funktionen.
- Wave-3-Sunset abgeschlossen: lokale `ModuleNotFoundError`-Fallback-Zweige wurden aus den zentralen UI-Contract-Bridges (`bw_libs/ui_contract/keybinding.py`, `bw_libs/ui_contract/popup.py`, `bw_libs/ui_contract/hsm.py`, `bw_libs/ui_contract/laufkern.py`) entfernt; Shared-Imports sind jetzt verpflichtend.
- Guardrail-Sunset auf Wave-3 umgestellt: `tools/ci/check_ai_guardrails.py` blockiert `except ModuleNotFoundError` jetzt repo-weit in den Scan-Scopes (`app`, `bw_libs`) ohne Bridge-Allowlist.
- Wave-2-Sunset-Gate aktiviert: `tools/ci/check_ai_guardrails.py` erlaubt `except ModuleNotFoundError` nur noch in den zentralen UI-Contract-Bridges (`bw_libs/ui_contract/keybinding.py`, `bw_libs/ui_contract/popup.py`, `bw_libs/ui_contract/hsm.py`, `bw_libs/ui_contract/laufkern.py`) und blockiert neue lokale Fallback-Zweige ausserhalb dieser Baseline.
- LaufKern-Runtime-Auswertung in den produktiven Shortcut-Debug-Flow integriert: `app/adapters/gui/main_window.py` baut jetzt ein Manifest aus der Runtime-Shortcut-Registry, validiert es zentral und zeigt die aktuelle Intent-Reachability im Debug-Summary.
- LaufKern-Manifestaufbau aus dem Main-Window in einen dedizierten Provider ausgelagert: `app/adapters/gui/laufkern_manifest_provider.py` erzeugt jetzt den deklarativen Runtime-Manifestzustand aus der Shortcut-Registry.
- LaufKern-Tracking an produktiven Shortcut-Dispatch angebunden: `app/adapters/gui/main_window.py` protokolliert ausgefuehrte Runtime-Intents jetzt als LaufKern-Tracking-Artefakte (done/failed) und zeigt den Completion-Status aus der Artefaktaggregation im Runtime-Debug.
- LaufKern-Bridge eingefuehrt: neues zentrales Modul `bw_libs/ui_contract/laufkern.py` (Shared-`bw_gui.laufkern`-Bridge mit lokalem Fallback) und Export ueber `bw_libs/ui_contract/__init__.py`; zusaetzlicher Regressionstest `tests/test_laufkern_bridge.py` fuer Manifestaufbau und Shortcut-basierte Reachability.
- Step-5-Rollout gestartet: GUI-Bootstrap in `app/app.py` initialisiert das Root-Window jetzt ueber den zentralen Runtime-Host `bw_gui.runtime.TkRootHost` statt direkter `ui.Tk()`-Erzeugung.
- Step-6-Guardrail-Hardening umgesetzt: `tools/ci/check_ai_guardrails.py` blockiert jetzt zusaetzlich repo-weit lokale Neudefinitionen der reservierten Shared-Primitives `TkRootHost`, `ScrollablePopupWindow` und `WrappedTextField`.
- Korrektur-Annotationen erweitert: neue Durchdruecken-Funktion erzeugt bereichs-/aufgabenbezogene Sync-Gruppen ueber alle Personen; beim Deaktivieren werden Fremdkopien entfernt und nur die aktuelle Personen-Markierung lokal behalten.
- Sync-Bearbeitung erweitert: Farb-, Groessen- und Rotationsaenderungen propagieren jetzt gruppenweit; Alt-Drag erlaubt positionslokale Entkopplung eines Gruppenmitglieds ohne Einfluss auf gruppenweite Stilattribute.
- Annotation-Zwischenablage erweitert: Strg+C/Strg+X/Strg+V fuer Markierungen in der Korrekturansicht (Cut entfernt bei Sync die gesamte Gruppe, Paste setzt im Zielkontext auf gleicher relativer Position im Bereichsclip).
- Runtime-Einstellungen erweitert: persistente Defaults fuer Markierungsfarbe und PDF-Schriftgroesse (`default_annotation_color`, `default_annotation_pdf_font_size`) sind im Settings-Dialog konfigurierbar und wirken auf neue Annotationen.
- PDF-Annotation-Writeback praezisiert: FreeText-Writeback nutzt jetzt explizite Zentrierung (`align=center`) und stabile Fontvorgaben (`helv`), um Vorschau-/Export-Versatz bei Symbolen und Kommentaren zu reduzieren.
- Export-Filter gehaertet: Markierungen ausserhalb des aktiven Bereich-Clip werden beim `PDF ueberschreiben` gezielt ignoriert und als Warnliste gemeldet statt ungeprueft mitzuschreiben.
- Annotationsmodell erweitert: `pdf_annotations` speichert jetzt optional `area_code`, damit Korrektur-Overlay und Exportbereich robust auf denselben Bereich begrenzt werden koennen.
- Korrektur-UX verfeinert: Rotations-Buttons drehen jetzt semantisch korrekt (links/rechts) in stabilen 90-Grad-Schritten; PDF-Writeback nutzt denselben 90-Grad-Pfad fuer verlaessliche Darstellung in externen Viewern.
- Korrektur-Preview entkoppelt vom PDF-Annotation-Layer: Basisrendering blendet PDF-Anmerkungen aus, sodass nur das editierbare Korrektor-Overlay sichtbar bleibt (keine Doppelanzeige nach PDF-Ueberschreiben).
- Korrektur-Layout bereinigt: Export-Button aus der Korrekturansicht in die Klausur-Details verschoben; manueller `Punkte speichern`-Button entfernt; `Kommentare speichern` in `PDF ueberschreiben` umbenannt.
- Korrektur-Autosave gehaertet: bei manuellem Aufgaben-/Bereichswechsel werden aktuelle Punkte und Kommentare vor dem Wechsel sofort persistiert.
- PDF-Writeback stabilisiert (Windows): vor dem Ersetzen der Originaldatei werden offene Preview-Handles aus dem Dokument-Cache explizit geschlossen, damit `os.replace(...)` nicht mehr an geoeffneten Dateien scheitert.
- Marker-Tools erweitert: zusaetzliche Farbe `Pink` sowie Transformationsaktionen fuer ausgewaehlte Markierungen (Groesse +/- und Rotation links/rechts) inkl. persistenter Speicherung (`font_size`, `rotation_deg`) und PDF-Writeback.
- Korrekturansicht erweitert: Symbol-Markierungen und Freitext-Einfuegen direkt im Vorschau-Canvas (Farbauswahl, Platzieren per Klick, Verschieben per Drag, Loeschen per Entf) mit persistenter Speicherung im Exam-JSON (`pdf_annotations`).
- PDF-Writeback erweitert: `Kommentare speichern` schreibt die app-generierten Markierungen direkt in die Original-PDFs (sichtbar in externen PDF-Tools) und entfernt dabei zuvor app-generierte Marker bei erneutem Speichern.
- Korrektur-Workflow erweitert: pro Person+Aufgabe koennen jetzt persistente Freitextkommentare erfasst werden (`task_comments` im Exam-JSON) inklusive Immediate-Save und Undo/Redo.
- Korrektur-Shortcut angepasst: Fertig-Toggle liegt jetzt auf `Strg+Leertaste`, damit `Leertaste` in Eingabefeldern konfliktfrei bleibt.
- Korrektur-Eingabefluss gehaertet: Punktefeld wird jetzt auch bei Korrekturstart sowie bei Aufgaben-/Bereichswechsel automatisch fokussiert und voll selektiert.
- Korrekturmaske erweitert: neues Aufgaben-Kommentarfeld (neben Punkten) mit Speichern auf Fokusverlust/Commit.
- Export-Flow erweitert: neuer Punkte-CSV-Export (Menue + `Strg+E` + Export-Button) schreibt Zeilen pro Person mit Aufgaben als Spalten; erste Datenzeile enthaelt die Maximalpunkte je Aufgabe.
- Korrekturansicht um Vorschau-Zoom erweitert (10%-Schritte, Grenzen 10-240): Zoom-Buttons `-`/`+`, Reset auf `100%`, Shortcuts `Strg+Mausrad`, `Strg++`, `Strg+-` und `Strg+0` analog zum Blattwerk-Preview-Verhalten.
- Korrekturmodus-Shortcut ergaenzt: `Leertaste` toggelt jetzt robust `Fertig korrigiert` (auch bei Fokus im Punktefeld) und nutzt dafuer einen eigenen zentralen Runtime-Intent.
- Detail-Hub semantisch bereinigt: die Bereichszeile zeigt jetzt eindeutig `Fertig korrigiert X/Y`; `Aktive Person` wird in der Detailansicht nicht mehr angezeigt, sondern nur in Arbeitsansichten (Einlesen/Korrektur).
- Extraseiten-Editor ueberarbeitet: die Tabelle zeigt jetzt `Bereich | Aufgaben | Seite` statt PDF-Spalte und startet seitenlokal leer, wenn fuer die aktuelle Extraseite noch keine Zuordnung existiert.
- Extraseiten-Workflow ergaenzt: oberhalb der Extraseiten-Tabelle wird eine kompakte Uebersicht aller vorhandenen Standardbereiche mit Aufgaben eingeblendet.
- View-Hierarchie vereinheitlicht: globale Kopfzeile entfernt, stattdessen klare Ueberschriften je View (`Uebersicht`, `Klausur-Details`, `Einlesen`/`Extraseiten`, `Korrektur`).
- Korrektur-Navigation per Tastatur erweitert: links/rechts wechselt Person, hoch/runter wechselt Aufgabe, Strg+hoch/runter wechselt Bereich.
- Korrektur-Workflow um person+bereichsbezogenen Fertigstatus erweitert: `Fertig korrigiert` wird erst bei vollstaendig bewerteten Aufgaben aktiv und sperrt danach die Punkteingabe fuer diese Kombination.
- Detailanzeige `Bereiche` erweitert: zusaetzlich zur Korrekturquote wird jetzt angezeigt, wie viele Bereiche bei allen Personen als fertig markiert sind.
- Bereichs-Neuziehen erweitert: der One-Shot-Overwrite-Flow funktioniert jetzt auch fuer Extraseiten-Zuordnungen.
- Bereichslisten-Selektion im Einlese-/Extraseiteneditor haertet den Canvas-Highlight-Flow: ausgewaehlte Bereiche werden links sofort sichtbar hervorgehoben.
- Einlese-Editor erweitert: neuer One-Shot-Flow `Bereich neu ziehen` erlaubt das gezielte Ueberschreiben eines ausgewaehlten Standardbereichs mit der naechsten gezogenen Box (inkl. Seitenwechsel).
- Einlese-Rendering erweitert: optionaler Superseitenmodus ueberlagert alle verfuegbaren PDFs einer Seite als pixelweise Dunkel-Superposition (dunkel gewinnt), um bereichsuebergreifende Markierungen robuster auszurichten.
- Datenmodell auf zukunftsorientiertes Trennschema umgestellt: `regions` enthaelt nur noch Standard-Templatebereiche (nicht studentgebunden), Extraseiten-Zuordnungen werden separat in `extra_page_assignments` gepflegt.
- Legacy-Kompatibilitaet bewusst entfernt: JSON-Dateien ohne `extra_page_assignments` werden als nicht unterstuetztes Altschema abgewiesen.
- Repository-/Domain-Tests erweitert fuer das neue Vorwaerts-Schema (Akzeptanzfall + harte Ablehnung von Legacy-Strukturen).
- Korrekturmodus begonnen umzubauen: eigene Korrekturansicht mit getrennter Arbeitsflaeche (statt eingebettetem Schnellkorrektur-Block in der Detailansicht).
- Korrekturansicht nutzt bereichsbasierte Vorschau-Ausschnitte je Schueler:in (Koordinaten aus dem eingelesenen Standardbereich werden auf das aktuelle PDF angewandt).
- Extraseitenfluss gehaertet: Aufgabenpflege fuer Extraseiten wird unterbunden; Extraseiten duerfen nur bestehenden Standard-Bereichen zugeordnet werden.
- Sessionweite Undo/Redo-History eingefuehrt: Inhaltsmutationen werden jetzt zentral als Aktionen registriert und sind per `Strg+Z`/`Strg+Y` sowie ueber das neue `Bearbeiten`-Menue rueckgaengig/wiederholbar.
- Undo-Guardrails erweitert: `tools/ci/check_ai_guardrails.py` erzwingt Undo/Redo-Vertraege in `ui_intent_controller.py` und prueft die Menue-/Shortcut-Verdrahtung (`Bearbeiten`, `Strg+Z`, `Strg+Y`).
- Extraseiten-Editor parity hergestellt: Im Extraseitenmodus koennen Bereiche jetzt direkt gezogen, mit Aufgaben gespeichert und wieder geloescht werden (gleiches Editor-Panel wie im Einlesemodus).
- Step-2-Guardrail-Scope abgeschlossen: die repo-weite GUI-Vertragspruefung in `tools/ci/check_ai_guardrails.py` umfasst jetzt zusaetzlich `bw_libs/`, sodass direkte `tkinter`/`ttk`-Imports und neue lokale `ui`/`widgets`/`tui`-Basisklassen auch in Shared-Library-Pfaden blockiert werden.
- Einlese-Editor verbessert: unter dem aktiven Aufgaben-Eingabefeld werden jetzt modusabhaengige Formatbeispiele angezeigt (`Schnell`: `5B:2;5C:1`, `Formular`: zeilenweise `5B:2` / `5C:1`).
- Step-3-Exemption-Abbau fortgesetzt: Future-Entrypoint-Baseline fuer `app/adapters/gui/main_window.py` entfernt; der Exemption-Backlog ist fuer Korrektor jetzt leer (nur `none`-Marker).
- Step-3-Exemption-Governance aktiviert: `docs/GUI_MIGRATION_BACKLOG.md` ist jetzt verbindliche Referenz fuer aktive GUI-Baselines/Exemptions inkl. `remove_by`-Datum; Guardrails validieren die Backlog-Referenzen explizit.
- Klausur-JSON-Ablagepfad als persistente App-Einstellung erweitert: neuer Settings-Flow speichert `exam_index_dir` in `%APPDATA%/<app_name>/settings.json` und schaltet das aktive JSON-Repository zur Laufzeit auf den konfigurierten Ordner um.
- Klausurverwaltung erweitert: eigener Delete-UseCase/Repository-Flow fuer das Loeschen ausgewaehlter Klausur-JSON-Dateien aus dem aktiven Indexordner.
- UI-Flow umgebaut: bisherige SplitView/PanedWindow-Detailkopplung entfernt; Start zeigt nur die Uebersicht und wechselt explizit zwischen Uebersicht, Klausur-Detail und Einleseansicht.
- Detailansicht entschlackt: eigene Detail-Buttonzeile (`Zur Übersicht`, `Einlesen`, `Extraseiten`, `Korrektur`) ohne integrierte PDF-Vorschau.
- Einleseansicht als eigene Arbeitsflaeche entkoppelt (inkl. `Zurueck zur Klausur`-Aktion mit Esc-aequivalenter Rueckkehr).
- Canvas-Interaktion im Einlesen gehaertet: Mauskoordinaten werden scrolloffset-korrekt ueber `canvasx/canvasy` verarbeitet, sodass Rahmen trotz Scrollposition korrekt verfolgt werden.
- Regionen-Workflow geaendert: Markierungen erzeugen zunaechst Draft-Regionen (ohne Sofort-Validierungsfehler); Validierung auf Pflicht-Aufgaben erfolgt erst bei explizitem Speichern.
- Regionenliste erweitert: Draft-Regionen werden sichtbar gefuehrt und koennen ohne JSON-Schreibzugriff bearbeitet oder geloescht werden.
- Tests erweitert: neue Unit-Tests fuer JSON-Exam-Repository (Delete/Indexwechsel) sowie fuer persistente App-Settings (`exam_index_dir`).
- Governance-Policy geschaerft: `AGENTS.md` und `.github/copilot-instructions.md` enthalten jetzt explizit die Strict-bw-gui-only-Regel (keine lokale tkinter/ttk-Widgetimplementierung in Repos; wiederverwendbare GUI-Bausteine zuerst in bw-gui).
- Repo-weite Strict-bw-gui-Guardrails eingefuehrt: `tools/ci/check_ai_guardrails.py` scannt jetzt alle GUI-Pythondateien unter `app/adapters/gui/` per AST, blockiert direkte `tkinter`/`ttk`-Imports und verhindert neue lokale Basisklassen auf `ui`/`widgets`/`tui`.
- Future-App-Guardrails erweitert: `tools/ci/check_ai_guardrails.py` prueft jetzt zusaetzlich neue GUI-Entrypoint-Dateien (z. B. neue `main_window.py`/`ui.py`) auf verpflichtenden Shared-GUI-Bootstrap (`ensure_bw_gui_on_path`, `bw_gui.runtime`, Shared-Menu/Dialog/Shortcut/Hover) und blockiert direkte `tkinter`-Imports.
- Guardrails gehaertet: `tools/ci/check_ai_guardrails.py` erzwingt jetzt in `app/adapters/gui/main_window.py` verpflichtende Shared-UI-Contracts (Menue/Dialog/Hover) und blockiert Legacy-Fallbackmuster (`ModuleNotFoundError`/`None`-Guards).
- Nicht-Theme-Sonderpfade entfernt: `app/adapters/gui/main_window.py` bindet Shared-Menue, Shared-Hover-Formatter und Shared-Settings-Dialog jetzt verpflichtend; optionale `ModuleNotFoundError`-/None-Guards wurden entfernt.
- Theme-Sonderpfade entfernt: `app/adapters/gui/main_window.py` nutzt fuer Style-Aufbau und Token-Ableitung jetzt nur noch den verpflichtenden Shared-Pfad (`bw_gui.theming`) ohne callable-/Hex-Fallback-Zweige.
- Canvas-Theme-Sync erweitert: Leseansicht und Extraseiten-Popup beziehen ihre Canvas-Hintergrund-/Rahmenfarben jetzt aus Shared-Theme-Tokens und werden bei Style-Refresh neu angewendet.
- Welle-13-Pilot gestartet: `app/adapters/gui/main_window.py` nutzt fuer die Haupt-Widgetstyles jetzt die Shared-Theme-Basis aus `bw_gui.theming` (`apply_window_theme`, `configure_ttk_theme`, `get_theme`) statt fixer Hex-Farben.
- Theme-Reapply verdrahtet: beim Commit aus dem Shared-Settings-Dialog wird `_build_styles()` erneut ausgefuehrt, sodass Tooltip-/Menu- und Widget-Theme im selben Schritt aktualisiert werden.
- Hover-Contract weiter ausgebaut: zusaetzliche Buttons in Korrektur-/Einlese-Workflows, Bereichsaktionen und Extraseiten-Popup-Navigation nutzen jetzt `SharedHoverTooltip` mit konsistenten Action-/Shortcut-Hinweisen.
- Shared-Menueleiste integriert: `app/adapters/gui/main_window.py` nutzt jetzt primaer `bw_gui.menu.CustomMenuBar` mit provider-basierten Menues (`Datei`/`Modus`/`Debug`) fuer zentrale Datei-, Modus- und Runtime-Debug-Aktionen.
- Menu-Theme-Sync verdrahtet: bei Settings-Commit aktualisiert `_apply_settings_dialog_payload` jetzt auch das Theme der Shared-Menueleiste, sodass Tooltip- und Menu-Theme konsistent wechseln.
- Hover-Contract im Hauptlayout ausgebaut: zentrale Action-Buttons (oeffnen/einlesen/extraseiten/debug) und Modus-Umschalter in `app/adapters/gui/main_window.py` nutzen jetzt konsequent `SharedHoverTooltip` mit einheitlicher Shared-Shortcut-Formatierung.
- Shared-Settings-Dialog integriert: `app/adapters/gui/main_window.py` oeffnet jetzt einen tab-basierten `bw_gui.dialogs.open_tabbed_settings_dialog`-Flow fuer Tooltip-Theme, Detail-Zuordnungsmodus und Runtime-Offline-Simulation.
- Einstellungs-Entry im UI erweitert: neue Aktion `Einstellungen` in der Hauptaktionsleiste startet den Shared-Dialog und schreibt Commit-Werte in die bestehenden Runtime-Variablen/Handler zurueck.
- Shared-Tooltip-Polish uebernommen: `bw_gui.widgets.HoverTooltip` zeigt Hinweise jetzt verzoegert, ermittelt den aktiven Theme-Kontext aus dem Host-Fenster und haelt Tooltip-Positionen per Bildschirm-Clamping sichtbar.
- Shared-Theme-Feinschliff uebernommen: Settings-/Sidebar-Stile und verfeinerte Scrollbar-Kontraste stammen jetzt aus dem aktualisierten `bw-gui`-Submodule-Stand.
- Hover-Overlay-Formatierung harmonisiert: `app/adapters/gui/main_window.py` nutzt jetzt `bw_gui.shortcuts.compose_hover_text(...)` und setzt einen konsistenten Shared-Tooltip-Theme-Key fuer Aktionshinweise.
- Tk-Runtime-Pilotmigration erweitert: `app/app.py` initialisiert das Root-Window jetzt ueber `bw_gui.runtime.ui` statt direktem `tkinter`-Import.
- Tk/ttk-Runtime-Pilotmigration gestartet: `app/adapters/gui/main_window.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk-Runtime-Pilotmigration erweitert: `bw_libs/app_shell.py` nutzt jetzt `bw_gui.runtime.ui` statt direktem `tkinter`-Import.
- Shared-Dialogmigration gestartet: zentrale Bridge `app/adapters/gui/dialog_services.py` auf `bw_gui.dialogs` eingefuehrt; `ui_intent_controller.py` und `main_window.py` nutzen jetzt Shared `messagebox`/`simpledialog`/`filedialog`-Services statt direkter `tkinter`-Dialogimports.
- Pilotmigration zum gemeinsamen GUI-Core gestartet: `bw-gui` als Git-Submodule eingebunden und `bw_libs/ui_contract/*` via Bridge auf `bw_gui.contracts.*` umgestellt, sodass Keybinding-/Popup-/HSM-Vertraege aus der gemeinsamen Quelle geladen werden.
- Testsuche gehaertet: `pytest.ini` begrenzt die Sammlung auf `tests`, damit Submodule-Tests nicht unbeabsichtigt in Korrektor-Laeufe einfließen.
- Shortcut-Hinweis aus Button-Labels entfernt: `Neue Klausur` zeigt kein `(Strg+N)` mehr im Label; der Hinweis liegt jetzt im Hover-Overlay (Shared `HoverTooltip`).
- G5 abgeschlossen: AppIdentity-Manifest `app/app_info.py` eingefuehrt und im GUI-Bootstrap (`AppDependencies`) als zentrale Quelle fuer Startup-/Window-Metadaten verdrahtet.
- G3/G4 gestartet: `build_gui_dependencies()` liefert jetzt eine zentrale Shell-Konfiguration; Startup und Hauptfenster folgen dem `AppDependencies`-Konventionspfad und nutzen `bw_libs/app_shell.py`.
- G2.1 gestartet: Shared-Modul `bw_libs/app_paths.py` eingefuehrt (AppPaths-Discovery sowie atomische JSON/Text-Write-Helfer).
- Persistenz-Pilot: `app/infrastructure/repositories/file_utils.py` bridged atomische JSON-Writes auf die zentrale `bw_libs/app_paths.py`-API.
- Guardrails beruecksichtigen `bw_libs/app_paths.py` als relevanten Shared-Pfad.
- UI-Contracts fuer Keybindings, Popup-Lifecycle und HSM wurden auf das Shared-Paket `bw_libs/ui_contract/` umgestellt; GUI und Tests importieren die Vertraege jetzt zentral statt aus lokalen Duplikatmodulen.
- Guardrails/Governance wurden auf `bw_libs/ui_contract`-Pfade umgestellt; `bw_libs/` wird bei Changelog-/Development-Log-Relevanz mitgeprueft.
- HSM-Contract-Modul `app/adapters/gui/hsm_contract.py` eingefuehrt (Intent-/Payload-Validierung, Transition-Regeln, Escape-Resolver).
- Zentralen Intent-Katalog fuer Runtime-Shortcuts in `app/adapters/gui/ui_intents.py` eingefuehrt.
- `app/adapters/gui/main_window.py` validiert Runtime-Shortcut-Intents jetzt gegen den HSM-Contract und blockiert unbekannte Intents frueh.
- Escape-Verhalten folgt jetzt zentraler Priorität: erst aktives Popup schliessen, dann Inline-/Submodus verlassen, danach Rückkehr zur Übersicht.
- Tests `tests/test_hsm_contract.py` fuer Intent-Contract, Transition-Gates und Escape-Prioritätskette hinzugefuegt.
- Runtime-Debug-Popup ist jetzt als nicht mode-blockierendes Parallel-Popup (`dialog.non_blocking`) registriert; Dialog-Priorität in der Runtime-Aufloesung basiert nur noch auf mode-blockierenden Popups.
- Wave-1-Hardening: Popup-Lifecycle wurde in `app/adapters/gui/main_window.py` explizit ueber die zentrale `PopupPolicyRegistry` verdrahtet (Shortcut-Debug und Extraseiten-Popup inklusive direkter open/close-Registrierung).
- Guardrails erweitert: `tools/ci/check_ai_guardrails.py` prueft jetzt die Runtime-Integration (`evaluate_runtime`) und PopupPolicy-Lifecycle-Hooks im Hauptfenster.
- Tests fuer zentrale Runtime-Module ergänzt: `tests/test_keybinding_registry_runtime.py` und `tests/test_popup_policy_registry.py`.
- Guardrail-Basis eingefuehrt: `AGENTS.md`, `.github/copilot-instructions.md`, PR-Template sowie `tools/ci/check_ai_guardrails.py` plus CI-Workflow.
- Zentrale UI-Basis fuer Tastatur- und Popup-Steuerung eingefuehrt: `app/adapters/gui/keybinding_registry.py` und `app/adapters/gui/popup_policy.py`.
- Governance erweitert: Feature-Arbeit wird als eigener Commit-Block gefuehrt, Push bleibt explizit manuell.
- Guardrails präzisiert: `CHANGELOG.md` wird nur bei nutzer- oder coentwicklerrelevanten Aenderungen erzwungen; Prozesswarnungen (Commit-/Push-Guidance) erscheinen nur lokal und nicht in CI.
- Wave-1-Start fuer den Hybrid-Resolver: `app/adapters/gui/keybinding_registry.py` enthält jetzt einen zentralen Runtime-Kontextvertrag (`KeybindingRuntimeContext`) und eine einheitliche `evaluate_runtime`-API fuer mode-/offline-/textfokus-/dialogbasierte Aktivierungspruefung.
- Wave-1 konkret verdrahtet: globale Shortcuts in `app/adapters/gui/main_window.py` laufen jetzt ueber den zentralen Runtime-Resolver statt direkter Bind-Dispatches.
- Tabellarische Runtime-Debug-Ansicht fuer Shortcuts im Hauptfenster ergänzt (`Shortcut Debug`, `Strg+Shift+D`) inkl. Offline-Simulation (`Strg+Shift+O`) und Aktiv/Disabled-Gruenden pro Modus.

## 2026-04-26
- Initiales Projekt `Korrektor` erstellt.
- Grundarchitektur mit Adapter/Core/Infrastructure umgesetzt.
- Klausur-Import aus PDF-Ordner mit PyMuPDF implementiert.
- JSON-Indexpersistenz und atomare Schreibvorgänge eingefuehrt.
- Erste Korrektur-Schnellansicht mit Sofortspeicherung in CSV integriert.
- Einlesemodus mit PDF-Seitenanzeige, Rechteck-Markierung per Maus und Bereichs-/Aufgabenzuordnung umgesetzt.
- Sofortige JSON-Speicherung jeder neu markierten Region beim Abschluss der Zuordnung eingebaut.
- Seitennavigation und Schueler:innen-Navigation im Einlesemodus ergänzt (Buttons + Pfeiltasten links/rechts im aktiven Einlesemodus).
- Abschlusslogik fuer Einlesemodus mit Warnhinweis bei unmarkierten Standardseiten hinzugefuegt.
- Separater Extraseiten-Modus hinzugefuegt: sequenzielle Navigation ueber alle Extraseiten inkl. Anzeige Person + Index + Gesamtzahl.
- Extraseiten koennen direkt einem oder mehreren Bereichen (A,B,...) zugeordnet werden; Zuordnung wird sofort in JSON persistiert.
- Vorhandene Extraseiten-Zuordnung derselben Person/Seite wird aktualisiert statt dupliziert.
- Bereichsbasierter Korrekturmodus hinzugefuegt: Navigation nur ueber Schueler:innen mit Zuordnung im gewählten Bereich.
- Extraseiten-Quick-View pro aktueller Person als Popup integriert.
- Escape-Navigation auf Ebenenmodell angepasst: Feldfokus -> Modus verlassen -> Detail verlassen -> Gesamtuebersicht.
- Extraseiten-Popup-Verhalten verfeinert: beim Fallwechsel im Korrekturmodus automatische Aktualisierung/Oeffnung, bei Modusende sauberes Schliessen.
- Beim Fallwechsel springt der Fokus jetzt immer in das erste Eingabefeld der Korrekturmaske.
- UX-Hotfix: PDF-Rendering in Einlese- und Extraseitenansicht stabilisiert (robuster Fehlerpfad, sichtbare Fehlermeldung statt stiller No-Op).
- Workflow-Update: Nach `Neue Klausur` startet der Einlesemodus automatisch.
- Detailansicht angepasst: Schnellkorrektur-Bereich ist ausserhalb des Korrekturmodus ausgeblendet und wird nur im aktiven Korrekturmodus angezeigt.
- Klausurliste verbessert: erste Zeile wird nach Refresh automatisch selektiert; Oeffnen per Enter/Doppelklick unterstuetzt.
- Repo-Hygiene: `.gitignore` fuer venvs, caches, lokale Laufzeitdaten und Konfigurationsreste angelegt.
- Start Modus-Umbau: Uebersicht und Detail werden jetzt als getrennte Fensterzustände geschaltet (Liste verschwindet in Detailmodus, Back-Navigation zurueck in Uebersicht).
- Einlesemodus erweitert: Scrollbares PDF-Viewport + Inline-Bereichsliste + Inline-Bereichseditor unter der Vorschau (kein Aufgaben-Popup mehr fuer Standardbereiche).
- Bereichslogik begonnen: neue Bereiche erhalten automatische Labels (A, B, ..., Z, AA, ...); Loeschen triggert kompakte Neuvergabe.
- Bereichsbearbeitung begonnen: Bereich per Klick aus Liste laden, Aufgaben/Punkte direkt inline speichern oder loeschen (inkl. JSON-Sofortspeicherung).
- Detailmodus weiter aufgesplittet: Einlesen/Korrektur/Extraseiten als explizite Submodi mit eigener Sichtbarkeitslogik im selben Detailfenster.
- Bereichsauswahl erweitert: Bereiche koennen nun direkt im Canvas per Klick auf das Rechteck ausgewählt und sofort inline bearbeitet werden.
- Inline-Speicherfluss gehärtet: Bereichsänderungen werden vor Seiten-/Personwechsel automatisch gespeichert.
- Reindex-Verhalten praezisiert: Beim Loeschen werden nur Standardbereiche neu benannt, Extraseiten-Zuordnungen bleiben erhalten.
